package tree

// ParseUpstream returns the PR number referenced by an `upstream: #N` link in
// a PR body, or 0 if none is present. Implemented in Task 3.
func ParseUpstream(body string) int {
	return 0
}
